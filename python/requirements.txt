# Python 依赖包列表 - 用于推理、训练服务和 PyO3 集成

# 深度学习框架
torch>=2.0.0
torchvision>=0.15.0

# 数据处理
numpy>=1.24.0
pandas>=2.0.0

# 数据库（可选：使用 banqi-data-collector 保存到 mongo 时才需要）
pymongo>=4.6.0

# 日志和监控
tensorboard>=2.15.0
wandb>=0.16.0  # 可选，用于实验跟踪

# 系统资源监控（CPU/内存/GPU，见 system_monitor.py）
# nvidia-ml-py 为官方维护的 pynvml（import 名仍是 pynvml）
psutil>=5.9.0
nvidia-ml-py>=12.0.0

# 工具
tqdm>=4.66.0
pyyaml>=6.0

# gRPC 通信依赖（自对弈Worker与训练Server解耦通信）
grpcio>=1.50.0
grpcio-tools>=1.50.0
protobuf>=4.20.0

# --- PyO3 + maturin 集成所需 ---
# maturin 用于编译 Rust PyO3 扩展 (不需要放到运行时 venv，但这里一起列出方便新手)
maturin==1.14.1
# setuptools-rust / wheel / typing 等通常 pip 自带，无需额外声明
